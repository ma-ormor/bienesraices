extends ../layout/admin

block styles  
  link(rel="stylesheet", href="https://cdnjs.cloudflare.com/ajax/libs/dropzone/5.9.3/dropzone.css")
  meta(name="csrf-token" content=csrfToken)

block contenido
  .py-10 
    h1.text-4xl.my-10.font-extrabold.text-center Bienes
      span.font-normal Raíces
    h2.text-center.text-2xl.font-bold=pagina

    .bg-white.shadow.py-8.px-4.rounded.mx-auto.max-w-4xl.my-10(
      class="md:px-10")
      form#imagen.mt-5.border-dashed.border-2.w-full.h-auto.rounded.flex-col.justify-center.items-center(
        class="dropzone min:h-96" method="POST" action=`/propiedades/agregar-imagen/${propiedad.id}`)
        input(type="hidden" name="_csrf" value=csrfToken)

      button#publicar.boton(
        class="hover:bg-indigo-700 cursor-pointer uppercase" 
        type="button") Publicar
block scripts
  script(src="/js/agregarImagen.js")